node_modules/
.expo/
dist/
web-build/
.env*
!.env.example
*.log
expo-env.d.ts

# Firebase Admin service-account keys — never commit these
serviceAccount*.json
*-service-account.json

# Local native modules: Gradle output
modules/*/android/build/

# Kept out of the repo on purpose: generated native project, session notes,
# and the Firebase Android config (regenerable from the Firebase console).
android/
ios/
SESSION-HANDOFF.txt
google-services.json

# Firebase hosting cache
.firebase/

# Agent worktrees, promo video takes/frames, and the regenerable Play import bundle
.claude/worktrees/
promo/*.mp4
promo/*.png
assets/store/achievements/play-achievements-import.zip
